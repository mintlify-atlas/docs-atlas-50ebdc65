---
title: Layout Components
description: Layout primitives for building flexible, responsive UIs
---

Rainbow's design system provides a comprehensive set of layout components built on flexbox primitives. These components enable you to build complex layouts with consistent spacing and alignment.

## Box

The foundational layout primitive. All other layout components are built on top of Box.

### Props

<ParamField path="background" type="BackgroundColor | 'accent'">
  Background color using design system tokens. Automatically sets color mode for nested elements.
</ParamField>

<ParamField path="padding" type="Space">
  Padding on all sides using spacing tokens (e.g., "12px", "20px")
</ParamField>

<ParamField path="paddingHorizontal" type="Space">
  Horizontal padding (left and right)
</ParamField>

<ParamField path="paddingVertical" type="Space">
  Vertical padding (top and bottom)
</ParamField>

<ParamField path="margin" type="NegativeSpace">
  Margin on all sides
</ParamField>

<ParamField path="borderRadius" type="number">
  Border radius in pixels
</ParamField>

<ParamField path="flexDirection" type="'row' | 'row-reverse' | 'column'">
  Flex direction for layout
</ParamField>

<ParamField path="alignItems" type="'flex-start' | 'flex-end' | 'center' | 'stretch'">
  Align children along the cross axis
</ParamField>

<ParamField path="justifyContent" type="'flex-start' | 'flex-end' | 'center' | 'space-between' | 'space-around'">
  Align children along the main axis
</ParamField>

<ParamField path="width" type="Width | number">
  Width (can use tokens like "1/2", "1/3" or pixel values)
</ParamField>

<ParamField path="height" type="Height | number">
  Height (can use tokens or pixel values)
</ParamField>

<ParamField path="shadow" type="Shadow">
  Shadow using design system shadow tokens
</ParamField>

<ParamField path="as" type="React.ComponentType">
  Render as a different component (e.g., Animated.View)
</ParamField>

### Usage

```typescript
import { Box } from '@/design-system';

function Example() {
  return (
    <Box 
      background="surfacePrimary" 
      padding="20px" 
      borderRadius={16}
      shadow="12px"
    >
      {/* Content */}
    </Box>
  );
}
```

### Polymorphic Component

Box supports polymorphic rendering with the `as` prop:

```typescript
import Animated from 'react-native-reanimated';

<Box as={Animated.View} style={animatedStyles}>
  {/* Animated content */}
</Box>
```

## Stack

Arranges child nodes vertically with equal spacing between them.

### Props

<ParamField path="space" type="Space" required>
  Space between children (e.g., "8px", "12px", "20px")
</ParamField>

<ParamField path="alignHorizontal" type="'left' | 'center' | 'right' | 'stretch'">
  Horizontal alignment of children
</ParamField>

<ParamField path="separator" type="ReactElement">
  Element to render between children
</ParamField>

<ParamField path="width" type="Width">
  Width of the stack container
</ParamField>

### Usage

```typescript
import { Stack, Text } from '@/design-system';

function Example() {
  return (
    <Stack space="12px" alignHorizontal="center">
      <Text size="20pt" weight="bold" color="label">
        Title
      </Text>
      <Text size="15pt" weight="regular" color="labelSecondary">
        Description
      </Text>
    </Stack>
  );
}
```

### With Separator

```typescript
import { Stack, Separator } from '@/design-system';

<Stack space="12px" separator={<Separator />}>
  <Item />
  <Item />
  <Item />
</Stack>
```

## Inline

Renders flowing content with equal spacing horizontally and vertically, wrapping to multiple lines if needed.

### Props

<ParamField path="space" type="Space">
  Space between children in both directions
</ParamField>

<ParamField path="horizontalSpace" type="Space">
  Horizontal space between children (overrides space)
</ParamField>

<ParamField path="verticalSpace" type="Space">
  Vertical space between children (overrides space)
</ParamField>

<ParamField path="alignHorizontal" type="'left' | 'center' | 'right' | 'justify'">
  Horizontal alignment
</ParamField>

<ParamField path="alignVertical" type="'top' | 'center' | 'bottom'">
  Vertical alignment
</ParamField>

<ParamField path="wrap" type="boolean" default="true">
  Whether to wrap items to multiple lines
</ParamField>

<ParamField path="separator" type="ReactElement">
  Element to render between children (only when wrap=false)
</ParamField>

### Usage

```typescript
import { Inline, Text } from '@/design-system';

function Tags() {
  return (
    <Inline space="8px" alignHorizontal="center">
      <Tag label="DeFi" />
      <Tag label="NFT" />
      <Tag label="Gaming" />
    </Inline>
  );
}
```

## Columns

Renders children in equal-width columns with consistent spacing.

### Props

<ParamField path="space" type="Space">
  Space between columns
</ParamField>

<ParamField path="alignHorizontal" type="'left' | 'center' | 'right'">
  Horizontal alignment of columns
</ParamField>

<ParamField path="alignVertical" type="'top' | 'center' | 'bottom'">
  Vertical alignment within columns
</ParamField>

### Usage

```typescript
import { Columns, Column, Text } from '@/design-system';

// Equal width columns
function EqualColumns() {
  return (
    <Columns space="12px">
      <Text size="15pt" weight="semibold" color="label">Column 1</Text>
      <Text size="15pt" weight="semibold" color="label">Column 2</Text>
    </Columns>
  );
}

// Custom width columns
function CustomColumns() {
  return (
    <Columns space="12px">
      <Column width="1/3">
        <Text size="15pt" weight="semibold" color="label">Sidebar</Text>
      </Column>
      <Column width="content">
        <Text size="15pt" weight="semibold" color="label">Main</Text>
      </Column>
    </Columns>
  );
}
```

## Rows

Renders children in equal-height rows with consistent spacing.

### Props

<ParamField path="space" type="Space">
  Space between rows
</ParamField>

<ParamField path="alignHorizontal" type="'left' | 'center' | 'right'">
  Horizontal alignment within rows
</ParamField>

<ParamField path="alignVertical" type="'top' | 'center' | 'bottom'">
  Vertical alignment of rows
</ParamField>

### Usage

```typescript
import { Rows, Row, Text } from '@/design-system';

function Layout() {
  return (
    <Rows space="12px">
      <Row height="content">
        <Text size="20pt" weight="bold" color="label">Header</Text>
      </Row>
      <Row>
        <Text size="15pt" weight="regular" color="label">Main content area</Text>
      </Row>
      <Row height="content">
        <Text size="13pt" weight="medium" color="labelSecondary">Footer</Text>
      </Row>
    </Rows>
  );
}
```

## Inset

Renders a container with padding.

### Props

<ParamField path="space" type="Space">
  Padding on all sides
</ParamField>

<ParamField path="horizontal" type="Space">
  Horizontal padding
</ParamField>

<ParamField path="vertical" type="Space">
  Vertical padding
</ParamField>

<ParamField path="top" type="Space">
  Top padding
</ParamField>

<ParamField path="bottom" type="Space">
  Bottom padding
</ParamField>

<ParamField path="left" type="Space">
  Left padding
</ParamField>

<ParamField path="right" type="Space">
  Right padding
</ParamField>

### Usage

```typescript
import { Inset, Text } from '@/design-system';

function Example() {
  return (
    <Inset space="20px">
      <Text size="15pt" weight="regular" color="label">
        Padded content
      </Text>
    </Inset>
  );
}
```

## Cover

Renders an absolutely filled container relative to its parent.

### Props

<ParamField path="alignHorizontal" type="'left' | 'center' | 'right'">
  Horizontal alignment of children
</ParamField>

<ParamField path="alignVertical" type="'top' | 'center' | 'bottom'">
  Vertical alignment of children
</ParamField>

<ParamField path="pointerEvents" type="'auto' | 'none' | 'box-none' | 'box-only'">
  Touch event handling
</ParamField>

### Usage

```typescript
import { Box, Cover, Text } from '@/design-system';

function Overlay() {
  return (
    <Box position="relative" height={200}>
      <Cover alignHorizontal="center" alignVertical="center">
        <Text size="17pt" weight="bold" color="label">
          Centered Overlay
        </Text>
      </Cover>
    </Box>
  );
}
```

## Spacing Tokens

The design system provides consistent spacing tokens:

```typescript
type Space = 
  | "2px" | "3px" | "4px" | "6px" | "8px" 
  | "10px" | "12px" | "16px" | "20px" | "24px" 
  | "28px" | "32px" | "36px" | "44px" | "52px" 
  | "60px" | "72px" | "80px" | "104px"
  | { custom: number };
```

### Custom Spacing

For values not in the token system:

```typescript
<Box padding={{ custom: 18 }}>
  {/* Content */}
</Box>
```